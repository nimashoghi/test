{
    "name": "@nimashoghi/test",
    "version": "0.0.2",
    "bin": {
        "nsh-test-postinstall": "dist/postinstall.js"
    },
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "license": "MIT",
    "scripts": {
        "build": "tsc --build",
        "watch": "tsc --build --watch",
        "prepublish": "yarn build",
        "publish:public": "yarn publish --access public"
    },
    "devDependencies": {
        "@types/jest": "^24.0.19",
        "@types/node": "^12.11.1",
        "fast-check": "^1.17.0",
        "jest": "^24.9.0"
    },
    "dependencies": {},
    "peerDependencies": {
        "fast-check": "^1.17.0"
    }
}
